const stylesheet=document.querySelector('link[href^="style.css"]');if(stylesheet)stylesheet.href='style.css?v=20260919-2';
const id=new URLSearchParams(location.search).get('id');const p=projects[id];const root=document.getElementById('detail');
if(!p){root.innerHTML='<h1>Project not found</h1><p><a href="index.html#projects">Return to projects →</a></p>'}else{
const evidence=(p.evidence||[]).map(x=>`<li>${x}</li>`).join('');const tags=(p.tags||[]).map(x=>`<span>${x}</span>`).join('');
const imgs=(p.images||[]).length?p.images.map((src,i)=>`<article class="project-card detail-shot"><div class="project-image"><img src="${src.replace('assets/projects/','')}" alt="${p.title} evidence ${i+1}"></div></article>`).join(''):`<article class="panel" style="grid-column:1/-1"><h3>Evidence pending</h3><p class="muted">${p.title==='Service Cloud Case Management'?'The project is completed. Real screenshots will be added when supplied.':'This project remains In Progress until implementation evidence is available.'}</p></article>`;
root.innerHTML=`<div class="kicker">PROJECT ${p.num}</div><div class="titleline"><h2>${p.title}</h2><span class="status ${p.class}">${p.status}</span></div><p class="detail-summary">${p.summary}</p><div class="detail-grid"><article class="panel"><h3>◈ Purpose</h3><p class="detail-text">${p.purpose}</p></article><article class="panel"><h3>⚙ Implementation</h3><p class="detail-text">${p.implementation}</p></article><article class="panel"><h3>▣ Business value</h3><p class="detail-text">${p.businessValue}</p></article><article class="panel"><h3>✓ What I configured / verified</h3><ul class="detail-list">${evidence}</ul></article><article class="panel"><h3>⚡ Admin concepts</h3><div class="chips">${tags}</div><p class="detail-text">${p.notes}</p></article><article class="panel"><h3>💬 Interview explanation</h3><p class="detail-text">${p.interview}</p></article></div><section style="margin-top:45px"><div class="kicker">VISUAL EVIDENCE</div><p class="muted">Actual Salesforce screenshots are shown below. Tap/click an image to view it clearly.</p><div class="detail-images">${imgs}</div></section><a class="btn" style="display:inline-block;margin-top:30px" href="index.html#projects">← Back to projects</a>`;
document.querySelectorAll('.detail-images img').forEach(img=>img.addEventListener('click',()=>{const o=document.createElement('div');o.className='image-modal';o.innerHTML=`<button aria-label="Close">×</button><img src="${img.src}" alt="${img.alt}">`;document.body.appendChild(o);o.addEventListener('click',e=>{if(e.target===o||e.target.tagName==='BUTTON')o.remove()})}));}
